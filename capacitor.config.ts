import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.limbertzdev.laaguilarsilva',
  appName: 'LaAguilarSilva',
  webDir: 'dist',
  server: {
    androidClearText: true
  }
};

export default config;
